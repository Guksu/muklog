# Sprint: 카피·미세 정합 스윕 (sprint-20260621-copy-fidelity) — S6+S7 통합

## 단일 기능
킷 SSOT 대비 사용자 대면 **카피 불일치 + 미세 비주얼**을 한 번에 정합. 순수 문자열/상수 변경(로직 무, 저위험). 각 변경은 해당 spec 단언도 함께 갱신(TDD).

## 변경 매핑 (현재 → 킷). 구현 시 각 문자열을 킷 라인과 재대조할 것.

### A. 초대코드 입장 (`JoinLogScreen.tsx`) — 킷 mk-home:226-238 / SPEC §2-2
| 위치 | 현재 | 킷 |
|---|---|---|
| SubBar(:48) | "초대코드 입장" | **"초대코드 입력"** |
| 제목(:64) | "연인의 로그에 입장하기" | **"연인의 로그에 들어가기"** |
| 본문(:71) | "연인이 공유한 6자리 초대코드를\n입력하면 같은 로그로 연결돼요." | **"연인이 보낸 6자리 코드를 입력하면\n같은 로그에서 함께 기록해요."** |
| 버튼(:84-85) | "입장하기" | **"들어가기"** |
| 성공 토스트 | (현재값 확인) | SPEC §2-2 **"로그에 들어왔어요! 💕"**(positive). 현재와 다르면 정합. |

### B. 로그 생성 완료 (`RoomCreatedScreen.tsx`) — 킷 mk-home:279-281
| 제목(:44) | "새 로그가 만들어졌어요" | **"우리 로그가 만들어졌어요"** |
| 본문(:52) | "아래 초대코드를 연인에게 보내면\n둘이 함께 기록할 수 있어요." | **"아래 코드를 연인에게 보내면\n둘이 함께 기록할 수 있어요."** |

### C. 시작 시트 (`AddSheet.tsx`) — 킷 mk-home:195-198
| 타이틀(:80) | "무엇을 할까요?" | **"어떻게 시작할까요?"** |
| 행1 desc(:85) | "혼자 시작하고, 나중에 초대해요" | **"먼저 시작하고 연인을 초대해요"** |
| 행2 title(:91) | "초대코드로 입장" | **"초대코드로 들어가기"** |
| 행2 desc(:92) | "연인이 보낸 6자리 코드 입력" | **"연인이 보낸 6자리 코드가 있어요"** |
| (행1 title "새 로그 만들기" = 킷 일치 → 불변. 주석 ":3·:18" 카피도 정합.) |

### D. 위시리스트 (`WishlistView.tsx`) — 킷 mk-extra:183-186·211
| 빈 제목(:61) | "가보고 싶은 곳을 모아요" | **"다음엔 여기 어때요?"** |
| 빈 본문(:68) | "다음 데이트에 가고 싶은 맛집을\n위시리스트에 담아두세요." | **"가보고 싶은 맛집을 미리 담아두면\n다음 데이트가 더 쉬워져요."** |
| 작성자 라벨(:19 부근) | "{닉}님이 추가" | **"{닉}님이 담았어요"** |
| (CTA "위시리스트에 추가" = 킷 일치 → 불변.) |

### E. 로그인 인트로 (`LoginScreen.tsx:32`) — 킷 mk-auth:96-98
| LOGIN_COPY | "데이트하며 다닌 맛집을\n사진·메모·위치로 둘이 함께 기록해요." | **"둘이 다녀온 맛집을\n오래오래 함께 기억해요."** |

### F. Stars 기본 크기 (`Stars.tsx:24`) — 킷 mk-ui:32
| 기본 size | 14 | **15** (소비처에서 명시 size를 주는 곳은 영향 없음 — 기본값만) |

## 제외/보류 (사유)
- **MemberBadge "혼자" 색**(fill/text-alternative vs RN surfaceAlt/fgWeak): RN에 가독성 우선 plan 결정 주석 존재(의도적). 카피 아님 → 이번 스윕에서 **변경 안 함**(문서화된 결정 존중, 필요 시 별도 논의).
- **Sheet 컨테이너 radius**: RN 이미 26으로 킷 일치(이전 감사 확인) → 변경 없음.

## 인수조건 (= 테스트, TDD)
- **AC1~F** 각 화면/컴포넌트가 위 "킷" 문자열/값으로 렌더(해당 spec 단언 갱신). 구 문자열 잔존 0(grep).
- **AC-reg** Stars 기본 size 15가 명시 size 미주입 소비처에만 영향(영향처 검토). 로직·동작 불변.
- **AC-pass** `npm test` green + `tsc --noEmit` 0. 회귀 0(문자열/상수만).

## 리스크
- spec이 구 문자열을 단언 → 반드시 같이 갱신(누락 시 red). Stars size는 스냅샷/레이아웃 단언 영향 가능 → 확인.
- 성공 토스트(A) 현재값이 이미 "로그에 들어왔어요"류면 무변경. 전역 토스트(S4) 경유 확인.
- 디바이스 스모크(카피 렌더)는 사용자 영역.

## 작업 목록
1. (pub, TDD) A~F 문자열/상수 변경 + 각 spec 단언 갱신 + ui-spec.md(킷 라인↔RN 매핑).
2. (qa-visual) 카피 킷 정합 전수 / (qa-logic) 테스트 green·로직 불변·구 문자열 잔존 0.
