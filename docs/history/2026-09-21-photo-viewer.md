# 기록 사진 전체화면 보기

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-09-21~23 |
| 브랜치 | feat/photo-viewer |
| PR | https://github.com/Guksu/muklog/pull/30 |
| 관련 경로 | src/components/PhotoViewer/, src/navigation/screens/MuklogDetailScreen/, src/theme/tokens/tokens.ts |

## 1. 개요

기록 상세의 사진을 눌러 전체 모습을 보고 1~3배로 확대하는 기능을 추가했다. 최초 작업 기준 브랜치에는 상세의 잘린 커버만 있었으나, PR 통합 시 최신 main에 전체화면 1차 구현이 들어온 것을 확인했다. 최종 변경은 기존 전체화면 계약에 확대·이동 기능을 통합한 것이다. 사용자 요청에 따라 실제 iOS 시뮬레이터 화면과 자동 테스트를 함께 검증하며, 손가락 제스처와 Android 실기기 확인은 별도로 구분한다.

## 2. 작업 내용

- 상세의 실제 사진을 누르면 해당 사진부터 전체화면으로 연다. 사진 없는 음식 커버는 열기 대상에서 제외한다.
- 전체화면은 사진 전체가 들어오는 크기로 표시한다. 닫기·사진 번호·이전/다음·확대/축소 버튼은 사진과 분리해 고정한다.
- 기존 Gesture Handler와 Animated로 사진 넘기기, 1~3배 확대, 확대 사진 이동을 구성한다. 확대 중에는 드래그로 다음 사진을 넘기지 않는다.
- 뷰어를 다시 열거나 사진을 바꾸면 확대 상태를 초기화한다. 열린 동안 사진 배열을 고정하고 이미지 로딩·실패 안내를 제공한다.
- 검정 배경과 흰 글자를 기존 팔레트에서 미디어 전용 토큰으로 정의한다. 새 패키지와 서버 변경은 없다.
- fe-skills의 pinch-zoom·swipe-dismiss-viewer·zoom-lightbox 패턴(MIT)을 검토했다. 초점 유지와 프레임별 ref 갱신 원칙을 RN으로 적용했으며, DOM 코드와 손을 떼면 확대를 원복하는 피드 동작은 사용하지 않았다.

- 2026-09-22 충돌 통합: 사용자 명시 승인으로 최신 main을 작업 브랜치에 병합했다. 기존 사진 타입·선택 index·접근성·Android 상단 여백 계약을 확대 구현에 통합하며, main의 조회 캐시·위시 피드백·에디터 보호 변경을 보존한다. 별도 브랜치의 중복 사진 뷰어 구현이 add/add 충돌의 원인이었다.

## 3. 검증 결과

| 검증 | 방법 | 결과 |
|------|------|------|
| 통합 전 전체 자동 테스트 | npm test -- --watchman=false --runInBand | 217 suites / 2466 tests 통과 |
| 타입 검사 | npm run typecheck | 통과 |
| 변경 공백 검사 | git diff --check | 통과 |
| 통합 전 로직·비주얼 독립 QA | 계약과 구현 교차검증 | 로직 4 suites / 59 tests 통과, 정적 비주얼 불일치 0 |
| iOS 실제 화면 | iPhone 17 Pro / iOS 26.4, 기존 사진 한 장 | 전체 사진 표시, 1→2→3→1배 버튼 조작, 최소/최대 버튼 비활성, 고정 닫기·하단 버튼, 확대 상태 닫기와 재진입 1배 복구 확인 |

검증 중 취소된 스와이프의 페이지 이동과 사진 전환 애니메이션 중단 시 조작 잠금 가능성을 발견해 수정했다. 취소·회전·이미지 오류 후 다시 이동하는 회귀 테스트가 통과했다. 최초 전체 실행의 핀치 테스트 2건 실패는 테스트 환경에서 제스처 활성화 등록을 기다리지 않던 문제였다. 활성화 반영 후 입력하도록 테스트를 고친 뒤 전체 재실행이 통과했다. 기존 중복 mock 및 Animated act 경고는 남아 있다.

2026-09-23 충돌 통합 후 전체 **231 suites / 2637 tests 통과**, 타입 검사·공백 검사 통과. API·접근성·Android 상단 여백·감소 모션 계약의 독립 정적 검토도 통과했다. 통합 중 구 ScrollView/즉시 닫기 전제의 테스트를 사진 순서·전환·닫기 완료를 확인하는 행동 테스트로 변경했다. 이후 전체 검사에서 RNTL 종료 훅 시간 초과가 여러 화면에 발생했으나, 별도 실행 41개와 최종 전체 재실행은 모두 통과했다. 시간 초과 원인을 제품 버그 또는 단순 부하로 확정하지 않는다.

통합본을 iOS 시뮬레이터에서 다시 불러와 실제 기록 사진 진입·전체 표시·2배 확대·닫기 후 동일 상세 복귀와 장소명/사진 순서 접근성 라벨을 재확인했다. 실제 다중터치·Android 검증 범위는 아래와 같다.

## 4. 확인 필요 · 후속

- 배포 전 iOS·Android에서 두 손가락 확대, 확대 중 이동, 손가락 수 전환, 여러 사진 넘기기를 직접 확인한다. 자동 조작 도구의 드래그가 시뮬레이터에서 변화를 만들지 못해 실제 제스처 통과로 판정하지 않았다.
- 현재 계정은 사진 한 장짜리 기록이므로 다중 사진 동작은 자동 테스트와 실제 기기 확인을 구분한다.
- Android 뒤로가기, 큰 글꼴·스크린리더, 회전 중 제스처 체감은 실기기 확인이 남는다.

## 5. 주의사항

이미지 주소는 상세에서 받은 기존 signed URL(일시 접근 주소)을 그대로 사용한다. URL 재발급·미리 불러오기·추가 Supabase/Kakao 호출은 없지만 캐시에 없는 이미지의 전송량은 발생할 수 있다. 확대는 원본의 해상도를 높이지 않는다. 기존 출시 버전에 배포하거나 OTA 업데이트를 발행하지 않는다.
