<!--
작업 기록 템플릿 — 하나의 PR(작업 단위)이 하나의 문서다.
- {중괄호} 자리를 채우고 이 안내 주석은 삭제한다.
- 섹션 구조(1~5)는 추가·삭제·개명하지 않는다. 형식이 통일되어야 다음 세션·다른 담당자가
  기록을 예측 가능하게 소비한다.
- 같은 PR에 커밋이 더 쌓이면 새 문서를 만들지 않고 이 문서를 갱신한다.
- 시크릿(토큰·키·비밀번호)은 기록하지 않는다. 설정값이 필요하면 키 이름만 적는다.
-->

# {작업명}

| 항목 | 내용 |
|------|------|
| 날짜 | {YYYY-MM-DD} |
| 브랜치 | {feat/...} |
| PR | {URL 또는 "미생성"} |
| 관련 경로 | {주요 파일·디렉토리} |

## 1. 개요

{무엇을 왜 했는가 — 배경·목표·요청 내용을 2~4문장으로. 처음 읽는 사람이 아래 내용을 이해할 수 있는 최소 맥락.}

## 2. 작업 내용

{변경·생성·삭제한 것을 경로와 함께. 여러 역할이 참여했으면 각 역할의 결과를 여기서 하나로 종합한다 — 역할별로 섹션을 나누지 않는다.}

- {경로 — 무엇을 어떻게, 왜}

## 3. 검증 결과

{실행한 검증(테스트·타입체크·린트·실행 확인)과 실제 결과. 실패가 있으면 숨기지 않고 그대로 적는다 — 통과만 적힌 기록은 다음 사람이 믿을 수 없다.}

| 검증 | 명령 | 결과 |
|------|------|------|
| {테스트} | {npm test} | {pass / fail — 상세} |

## 4. 확인 필요 · 후속

{사용자의 결정·승인이 필요한 것(선택지와 미결정 시 기본 동작 병기)과, 다음에 할 일(트리거와 담당 명시). 없으면 "없음"으로 명시한다.}

## 5. 주의사항

{다음 작업자가 알아야 할 것 — 제약, 미해결 이슈, 되돌릴 때 주의점. 없으면 "없음"으로 명시한다.}
