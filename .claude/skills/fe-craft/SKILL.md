---
name: fe-craft
description: "프론트엔드 제작 품질 — UI 디자인 8원칙 스크린샷 비평, 애니메이션·모션 리뷰, React/Next.js 성능 패턴으로 '동작하는' 화면을 '완성도 있는' 화면으로 끌어올린다. 'UI 다듬어줘/디자인 비평해줘/애니메이션 리뷰/모션 개선/리렌더·번들 최적화' 요청 시, 프론트 UI 구현·수정 작업 시, '다시 비평/재리뷰/개선안 적용' 등 후속 요청 시 반드시 이 스킬을 사용할 것. 배포 가부 판정은 fe-predeploy가 담당한다."
---

# FE Craft — 프론트엔드 제작 품질

> **muklog 적용 단서(RN 번역):** 이 스킬과 references는 웹(CSS/DOM) 기준으로 쓰였다 — guksu-harness v2.1.0에서 스킬째 복사(2026-09-01). muklog는 React Native이므로 **판단 기준은 그대로 쓰되 구현 수단은 번역한다**: `cubic-bezier(...)` → `Easing.bezier(...)` · `prefers-reduced-motion` → `AccessibilityInfo.isReduceMotionEnabled()` · "GPU 속성(transform/opacity)" → `useNativeDriver: true`가 가능한 transform/opacity만 애니메이트 · `transform-origin` → RN 미지원이라 translate 보정으로 근사. `references/react-performance.md`는 React 공통 항목만 유효하고 Next.js(번들·서버) 항목은 비적용. 비주얼 단일 출처는 여전히 킷 `templates/muklog`다 — 이 스킬은 킷이 침묵하는 영역(모션·비평 루프)의 기준으로만 쓴다. feSkills 스크립트 경로는 `.claude/scripts/feSkills.mjs`.

"동작한다"와 "완성도 있다" 사이의 간극을 메우는 스킬. AI가 만든 화면이 균질하게 무난해지는 이유는 기능 조립은 잘하지만 **눈이 없어서** 시각적 판단을 못 하기 때문이다 — 이 스킬은 그 판단을 (1) 디자인 8원칙 체크리스트, (2) 모션 리뷰 기준, (3) React 성능 규칙이라는 **언어화된 기준**으로 바꿔 비평·수정 루프를 돌린다.

## fe-predeploy와의 관계

| | fe-craft (이 스킬) | fe-predeploy |
|---|---|---|
| 시점 | 제작·수정 **중** (반복 루프) | 배포 **직전** (1회 게이트) |
| 산출 | 개선된 UI·코드 | pass/fail 판정 + 체크리스트 기록 |
| 질문 | "더 좋게 만들 수 있는가" | "배포해도 되는가" |

이 스킬은 판정하지 않고 개선한다. 배포 가부는 fe-predeploy 몫이다.

## 새 패턴을 만들기 전에 — fe-skills 라이브러리를 먼저 조회한다

이 스킬은 **비평하고 고치는** 쪽이고, [`Guksu/fe-skills`](https://github.com/Guksu/fe-skills)는 **패턴의 정본 코드**를 주는 쪽이다. 바텀시트·핀치줌·캐러셀처럼 이름이 붙은 UI 패턴을 새로 만들어야 하면, 사용자가 요청하지 않아도 직접 짜기 전에 라이브러리를 조회한다 — 이미 모션·접근성·`prefers-reduced-motion`이 정리된 코드에서 출발하면 비평 루프가 몇 바퀴 줄어든다.

```bash
node {harness 스킬 경로}/scripts/feSkills.mjs find "<사용자 요청 문장>"
node {harness 스킬 경로}/scripts/feSkills.mjs get <slug> --into <대상 디렉토리>
```

하네스가 구축된 프로젝트면 스크립트가 `.claude/scripts/feSkills.mjs`에 복사되어 있다. 설치는 필요 없다 — 필요한 스킬 하나만 가져오는 라이브러리다. 후보가 0건이거나 라이브러리를 못 가져오면 직접 구현하고 그 사실을 작업 기록에 남긴다. 가져온 패턴은 출처를 기록에 적는다.

## 세 영역 — 필요한 reference만 로드한다

| 영역 | 언제 읽나 | 파일 |
|------|----------|------|
| **디자인 비평** | UI 시각 품질을 다듬을 때, 스크린샷 비평 루프 실행 시 | `references/design-principles.md` |
| **모션 리뷰** | 애니메이션·트랜지션을 새로 넣거나 리뷰할 때 | `references/animation.md` |
| **React 성능** | React/Next.js 컴포넌트 작성·리팩토링·성능 개선 시 | `references/react-performance.md` |

## 스크린샷 비평 루프 (디자인 영역의 실행 방법)

코드만 읽고 "잘 됐다"고 판단하지 않는다 — 디자인은 렌더된 결과에만 존재한다. 검증 수단 서열의 "시각·실행 피드백"을 디자인에 적용한 것이다:

1. **렌더** — 실제 브라우저에서 화면을 띄운다 (브라우저 도구가 있으면 스크린샷 캡처, 없으면 사용자에게 스크린샷을 요청한다).
2. **비평** — `references/design-principles.md`의 8원칙 각각에 대해 관찰(무엇이 보이는가)과 위반(어떤 원칙이 깨졌는가)을 적는다. 원칙 이름을 명시한다 — 언어화가 곧 조향 수단이다.
3. **수정** — 상위 2~3개 위반만 고친다. 한 번에 전부 고치면 무엇이 효과였는지 알 수 없다.
4. **재렌더·비교** — 다시 캡처해 이전과 비교한다. 2~3회 반복이면 대부분 수렴한다.
5. **수렴 판단** — 8원칙에 뚜렷한 위반이 없으면 종료. 미세 취향 영역은 사용자 판단에 맡긴다(비평 결과를 선택지로 제시).

모션은 스크린샷으로 판정할 수 없다 — 느낌 판단이 필요한 모션은 사용자에게 실제 조작 확인을 요청하고, 코드 리뷰로 잡을 수 있는 위반(`references/animation.md`의 즉시 플래그 목록)은 코드에서 잡는다.

## 리뷰 출력 형식

리뷰 요청(코드·화면 비평)의 결과는 Before/After/Why 표로 낸다 — 지적만 있고 수정안이 없는 리뷰는 소비할 수 없다:

| Before | After | Why |
|---|---|---|
| {현재 코드/상태} | {수정안} | {근거 — 위반한 원칙·규칙 이름} |

표 뒤에 심각도순 요약과 (리뷰만 요청받은 경우) 적용 여부 질문을 붙인다. 수정까지 요청받았으면 바로 적용한다.

## 원천과 라이선스

세 reference는 외부 공개 자료를 증류·재구성한 것이다 — 출처와 라이선스는 각 파일 상단과 `LICENSES.md`에 명시되어 있다. 증류본과 업스트림이 충돌하면 업스트림이 우선한다(스냅샷 날짜 기준).

## 하네스 연동

프론트엔드(웹 UI) 도메인 하네스에서는 developer 에이전트(또는 도메인 스킬)에 "UI 구현·수정 시 fe-craft 스킬 참조"를 명시하고, 종료 게이트는 fe-predeploy가 담당한다. 독립적으로도 언제든 호출 가능하다.
